title: Entries
options:
  preview: http://68.183.58.246/{{ page.slug }}

sections:
  entriesList:
    headline: "All Entries"
    type: pages
    template: entry
    status: all
    info: "{{ page.authors }} {{ page.tags }} {{ page.date }}"
