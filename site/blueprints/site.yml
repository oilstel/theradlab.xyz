title: Site

sections:
  homepageProjects:
    label: Homepage Projects
    type: fields
    fields:
      homepageProjects:
        label: Lilypads
        type: pages
        layout: cards
        size: tiny
        query: site.find('projects').children.listed
        multiple: true
        info: "{{ page.title }}"
        
  recentProjects:
    label: Projects
    type: pages
    query: site.find('projects').children
    info: "{{ page.title }}"
    template:
      - project

  pages:
    label: Pages
    type: pages
    max: 3
    template:
      # - projects
      # - contact
      - about
