title: Projects

sections:
  projectsList:
    headline: "All Projects"
    type: pages
    template: project  # Replace with your specific template(s) if needed
    status: all
    info: "{{ page.authors }} {{ page.tags }} {{ page.date }}"
